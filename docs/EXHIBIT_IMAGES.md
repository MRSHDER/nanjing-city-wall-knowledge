# 展陈图片接入

当前已接入两张仓库内的真实照片：

- `public/images/liudehua-brick.jpg` —— 铭文城砖，用于「城砖铭文」节点的观察任务。
- `public/images/brick-display.jpg` —— 展厅城砖陈列，用于「城砖」节点的观察任务。

两张图都支持放大查看；未新增或替换首页、地图背景。

## 新增照片步骤

1. 将可使用的真实展陈照片放在 `public/images/`。
2. 在 `src/data/images.ts` 的 `exhibitImages` 中添加唯一 ID，以及 `src`、`alt`、`caption`。路径写 `images/文件名.jpg`，不要带开头斜杠；组件会处理 GitHub Pages 子路径。
3. 按用途接入对应节点：
   - 观察型任务（点击图片观察 → 反馈 → 才能继续）：在 `src/data/missions.ts` 对应任务的 `observation.imageId` 填写该 ID。
   - 图文说明型：在 `src/data/catalog.ts` 对应节点的 `imageIds` 填入该 ID。
   两种情况都不要修改节点 ID、missionId 或解锁关系。
4. 运行 `npm run build`，检查该节点默认图、放大图、关闭与错误提示。

## 待补素材清单（截至本轮）

下列素材来自南京城墙博物馆实地调研，**仓库中尚不存在**，只在任务里预留了位置和注释，**不要用 AI 生成图、网络图或无关背景图代替**。

| 编号 | 素材 | 关联任务 | 优先级 | 接入方式 |
| --- | --- | --- | --- | --- |
| A | 南京城墙博物馆「皇城 / 宫城」相关展陈照片 | `mission-four-walls`（四重城垣） | 中 | 作为该节点知识说明的一部分接入（`catalog.ts` 的 `imageIds`）；不建议做成纯装饰大图 |
| B | 南京城墙博物馆「正阳门（光华门）」展板照片 | `mission-city-gate`（城门） | 中 | 可作为该任务的观察对象，接入 `observation.imageId` |
| C | 南京城墙博物馆「城砖制作 / Production of Bricks」完整展板 | `mission-engineering`（筑城营造） | **最高** | 作为该任务的主要视觉内容，接入 `observation.imageId` |
| D | 铭文城砖实拍特写 | `mission-inscription`（城砖铭文） | **最高** | 优先替换或补充现有的 `liudehua-brick`，接入 `observation.imageId` |
| E | 《南京城墙砖产地及运输水系图》 | `mission-brick-origin`（城砖产地） | **最高** | 观察重点是「从哪里来」 |
| F | 同 E 的《南京城墙砖产地及运输水系图》 | `mission-ming-logistics`（大明物流） | **最高** | 与 E **共用同一张图**，观察重点是「怎样到南京」；不要为两关各准备一张不同地图 |
| G | 城门近现代变迁展板（武定门 / 雨花门 / 中华东门）**或**「明故宫变迁」时间线 | `mission-heritage`（城墙与今天） | 中 | 两者择一即可，不需要全部堆入页面 |

说明：

- C、D、E/F 为最高优先级，其中 **D 与 E/F 直接决定「从实物和展板找线索」这条叙事能否成立**。
- 地图类素材属于历史资料图，必须使用馆方展板、公开可靠来源或经授权的图片；在拿到素材之前，任务文案不引用具体图面内容。
- 接入图片时，`alt` 与 `caption` 应保留拍摄对象、来源及必要署名信息。

## 版本约束

现有详情面板为右侧窄面板，接入多张图片会增加页面高度。面板内部已支持纵向滚动（标题区固定），但同一节点的图片仍建议控制在 1–2 张，避免把任务推到屏幕很远之外。

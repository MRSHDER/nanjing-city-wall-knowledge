import { achievements } from './achievements'
import { catalog } from './catalog'
import { missions } from './missions'

export const knowledgeData = {
  catalog,
  missions,
  achievements,
}

export { catalog, missions, achievements }
